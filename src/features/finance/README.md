# Finance Feature Module

Use case:

- `application/classSaldoUseCase.ts`

Module ini menyimpan aggregate saldo kelas agar rumus pemasukan, pengeluaran, saldo bulanan, dan saldo terkini tidak tersebar di route.

Dokumentasi lengkap: `docs/features/class-profile/README.md`.
